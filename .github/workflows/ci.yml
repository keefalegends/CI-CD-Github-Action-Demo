name: CI + CD Example

on:
  push:
    branches: ["main"]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 18

      - run: npm install
      - run: npm test

  cd:
    needs: ci   # ⬅️ PENTING
    runs-on: ubuntu-latest

    steps:
      - name: Fake Deploy
        run: |
          echo "🚀 Deploying app..."
          echo "App deployed!"